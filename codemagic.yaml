# Codemagic CI/CD — SnapIQ iOS
# https://docs.codemagic.io/yaml-quick-start/building-a-flutter-app/

workflows:
  ios-testflight:
    name: iOS TestFlight (Otomatik)
    instance_type: mac_mini_m2
    max_build_duration: 60

    # Codemagic UI → Personal Account → Settings → Integrations → Developer Portal
    # → "Codemagic" entegrasyonu (Magnus/Matematikçik/Amin ile paylaşımlı)
    integrations:
      app_store_connect: Codemagic

    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
      # Variable group — Codemagic UI → App settings → Environment variables
      groups:
        - signing_credentials  # CERTIFICATE_PRIVATE_KEY bu grupta (paylaşımlı)
      vars:
        # ⚠️ App Store Connect → SnapIQ → App Information → Apple ID (sayı) ile değiştir
        APP_STORE_APP_ID: "PLACEHOLDER_SNAPIQ_APPLE_ID"
        BUNDLE_ID: "com.snapiq.snapiq"

    triggering:
      events:
        - tag
      tag_patterns:
        - pattern: 'v*'
          include: true

    scripts:
      - name: Initialize keychain
        script: |
          keychain initialize
      - name: Fetch signing files (auto-create profile if missing)
        script: |
          if [ -z "$CERTIFICATE_PRIVATE_KEY" ]; then
            echo "❌ HATA: CERTIFICATE_PRIVATE_KEY env var BOŞ veya yok!"
            echo "Codemagic UI → App settings → Environment variables → signing_credentials grubu."
            exit 1
          fi
          echo "✓ CERTIFICATE_PRIVATE_KEY uzunluğu: ${#CERTIFICATE_PRIVATE_KEY} karakter"
          echo "$CERTIFICATE_PRIVATE_KEY" | base64 --decode | tr -d '\r' > /tmp/cert_key.pem
          KEY_SIZE=$(wc -c < /tmp/cert_key.pem)
          echo "✓ Decoded key dosya boyutu: $KEY_SIZE byte"
          if [ "$KEY_SIZE" -lt 100 ]; then
            echo "❌ Decoded key çok küçük, base64 hatalı olabilir"
            exit 1
          fi
          # PKCS#8 → RSA traditional dönüşümü (gerekirse)
          if openssl pkey -in /tmp/cert_key.pem -traditional -out /tmp/cert_key_rsa.pem 2>/dev/null; then
            mv /tmp/cert_key_rsa.pem /tmp/cert_key.pem
            echo "✓ PKCS#8 → RSA formatına dönüştürüldü"
          else
            echo "ℹ️ Dönüşüm gerekmedi (zaten RSA formatında)"
          fi
          app-store-connect fetch-signing-files \
            "$BUNDLE_ID" \
            --type IOS_APP_STORE \
            --platform IOS \
            --certificate-key=@file:/tmp/cert_key.pem \
            --create
      - name: Add certificates to keychain
        script: |
          keychain add-certificates
      - name: Set up code signing settings on Xcode project
        script: |
          xcode-project use-profiles
      - name: Flutter packages + disable SwiftPM
        script: |
          flutter --version
          # google_mobile_ads CocoaPods kullanır, SwiftPM ile çakışır — kapat
          flutter config --no-enable-swift-package-manager
          flutter packages pub get
      - name: Pod install
        script: |
          find . -name "Podfile" -execdir pod install \;
      - name: Flutter build ipa
        script: |
          # Build number: HER ZAMAN Unix timestamp (sürekli artan, asla çakışmaz)
          BUILD_NUMBER=$(date +%s)
          echo "Using BUILD_NUMBER=$BUILD_NUMBER"
          flutter build ipa --release \
            --build-name=1.0.0 \
            --build-number=$BUILD_NUMBER \
            --export-options-plist=/Users/builder/export_options.plist

    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - flutter_drive.log

    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        submit_to_app_store: false
